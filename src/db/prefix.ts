import { PluginId } from '../constants'

export enum Index {
  Cart = `${PluginId}:idx:cart`,
}

export enum Prefix {
  Cart = `${PluginId}:doc:cart`,
}

export enum SchemaKey {
  Cart = `${PluginId}:scm:cart`,
}
